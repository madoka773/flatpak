# 你需要先把 `license/usr/lib/license/libuosdevicea.so` 放到 `~/.local/share/flatpak/runtime/org.freedesktop.Platform/CPU架构/23.08/active/files/lib/license/` 或者 `/var/lib/flatpak/runtime/org.freedesktop.Platform/CPU架构/23.08/active/files/lib/license/`

# 我这里不提供 `aarch64` 的 `libuosdevicea.so` ，如有需要自行寻找
